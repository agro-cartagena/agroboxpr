import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    iconContainer: {
        height: '100%',
        width: '100%',
    },

    icon: {
        height: '100%',
        width: '100%',
        resizeMode: 'contain'
    }
})

export default styles