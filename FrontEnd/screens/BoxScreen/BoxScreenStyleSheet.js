import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    arrow: {
        width: '20%',
        height: '30%',
        resizeMode: 'contain',
        marginTop: '10%',
    }

})

export default styles;