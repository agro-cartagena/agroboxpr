import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    screen: {
        padding: 10,
    },

    logoContainer: {
        width: '100%',
        height: '20%',
        alignSelf: 'center',

        marginTop: 20,
        marginBottom: 20
    }
})

export default styles;