import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    screen: {
        padding: 10,
    },

    cardContainer: {
        height: 175,
        margin: 12
    }
})

export default styles;