import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    logoContainer: {
        margin: 25
    },

    buttonContainer: {
        // margin: 10
    },

    button: {
        width: '100%',
        height: 50,

        alignSelf: 'center',
        marginTop: 5, 
        marginBottom: 5
    }
})

export default styles