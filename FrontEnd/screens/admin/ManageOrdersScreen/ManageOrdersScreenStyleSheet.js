import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    header: {
        color: 'white',
        fontSize: 22,
        fontWeight: "bold",
        textAlign: 'center',
        margin: 20
    },

    cardContainer: {
        marginTop: 25
    },

    card: {
        width: '90%'
    }
})

export default styles