import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    cardContainer: {
        marginTop: 25,
        marginBottom: 25
    },

    card: {
        height: 200,
        margin: 15
    }
})

export default styles;