import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({

    text:{
        fontSize: 22,
        fontWeight: 'bold',
        margin: 25,
        
        color:'white',
        textAlign:"center"
    },

    buttonContainer:{
        width: '65%',
        height: 50,

        alignSelf: 'center',
    }
})

export default styles